import classes from "./PageDivider.module.css"

const PageDivider: React.FC<{}> = (props) => {
    return (
        <div className = {classes["bottom-border"]}>
        </div>
    );
}
export default PageDivider